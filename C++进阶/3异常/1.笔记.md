当出现异常分支，返回值不能作为非法值判断的时候
throw 抛出异常

throw：关键字，在函数出现异常进行不下去的时候终止函数，抛出异常

当抛出异常没有被捕获的售后，默认调用terminate函数
terminate函数：终止程序

捕获异常：try catch
为了减少一次拷贝构造，catch一般用引用

栈解旋：当异常抛出后，程序会沿着函数调用链反向销毁已经构造的局部对象，直到找到能够处理异常的catch语句为止。
作用：保证资源释放；安全

noexcept:无异常声明