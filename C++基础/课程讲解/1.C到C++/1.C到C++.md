C++:1.所有的C语言文件都是C++文件
    2.文件后最为cpp 编译器程序为g++
名字控制：重名问题
命名空间：namespace    ::域解析符
using :使后面跟着的描述符生效

std:standard : C++标准命名空间

std::endl : 1、输出一个换行 2、刷新输出流(清空缓冲区)

struct加强
1、结构体名称可以单独作为类型出现
2、能够在结构体直接定义函数

bool类型：1.占一个字节内存
         2 赋任何非0 值都是1
    
左值：有内存可修改的值
右值：没有内存，不可修改的值 
三目运算符加强：   （）？ ：返回的是左值

const 值在符号表里,取地址时分配内存，是真常量
reference 引用:变量的别名，本质：指针常量
int&b = a;
int * const b = &a;
1.一定要初始化
2.引用一经过定义边不会修改朝向
const int&a //常引用：左值无法传递右值，不能修改
